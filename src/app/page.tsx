import ThemeToggle from "@/components/common/theme-toggle";

const Home = () => {
  return (
    <div className="dark:bg-black bg-red-500">
      <ThemeToggle />
    </div>
  );
};

export default Home;
